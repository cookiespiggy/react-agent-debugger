# dependencies
node_modules/

# next.js build output
.next/
out/

# typescript
*.tsbuildinfo
next-env.d.ts

# local runtime data — SQLite traces are state, never commit
data/

# IDE / agent workspace data (internal, not for the public repo)
.codebuddy/
.playwright-cli/

# docs assets generated locally are committed, but handoff is internal-only
handoff.md

# misc
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.env*.local
.env
.vercel
